/**
 * Server-only shared utilities and services
 * Use this import path: import { functionName } from '@/lib/shared/server';
 */

// Export image services (only available on server)
export * from './image-service'; 